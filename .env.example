# Copy to .env, replace [REDACTED] locally, then chmod 600 .env.
# Never commit the filled-in .env file.
NASANY_DOMAIN=
NASANY_EMAIL=
NASANY_TTY=/dev/ttyUSB2
NASANY_DUCKDNS_TOKEN=[REDACTED]
# Optional: set only when source-IP detection is not correct.
# NASANY_PUBLIC_IP=
